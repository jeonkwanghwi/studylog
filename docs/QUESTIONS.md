# 광휘에게 물어볼 것

자는 동안 진행하면서 쌓인 것들. **내가 임의로 정할 수 없거나, 계정·돈·법이 걸린 것만** 적는다.
기술 판단은 내가 결정하고 레저(`.superpowers/sdd/*/progress.md`)의 `Ruling:` 줄에 근거를 남겼다.

---

## 1. 계정 — 이게 없으면 결제가 한 줄도 검증이 안 된다

| | 상태 | 왜 필요한가 |
|---|---|---|
| Apple Developer Program ($99/년) | ? | 앱 등록, IAP 상품 생성, 실기기 테스트 |
| Google Play Console ($25 1회) | ? | 동일 |
| RevenueCat 계정 | ? | 결제 검증. 월 매출 $2,500까지 무료 |
| **Kakao Developers 앱** | ? | **카카오 로그인. REST API 키가 OIDC audience 로 쓰인다** |
| Anthropic API 키 | ? | **AI 판정. 지금 없어서 모든 사진이 관대 폴백으로 통과된다** |

지금 로컬 서버는 키가 없어서 판정이 사실상 꺼져 있다. 실제 판정 품질을 한 번도
못 봤다는 뜻이다 — 프롬프트가 종이책·노트북·태블릿을 제대로 구분하는지 검증이 안 됐다.

## 2. 앱스토어 가격 티어 확인 필요

상품표가 이 가격을 전제한다. **스토어에 없는 티어면 가격이 아니라 하루 배팅액을 조정**해야 한다
(참가비 = 일수 × 배팅액이 깨지면 "낸 만큼 돌려받는다"는 약속이 깨진다).

```
7,000  14,000  21,000  28,000  30,000  42,000
```

특히 **21,000 / 42,000** 이 한국 티어에 있는지 확인해달라. 없으면 하루 3,000원을
2,900원 같은 값으로 내려서 맞춘다.

## 2.5 소셜 로그인 — 카카오 추가함

애플·카카오만 하려 했다고 해서 확인해보니, **구글은 이미 되어 있었다** (애플과 같은
OIDC라 코드가 12줄 차이). 빼지 않고 셋 다 둔다.

**카카오를 서버에 추가했다.** `kauth.kakao.com` JWKS 로 검증하고, audience 는
Kakao Developers 의 **REST API 키**다. 앱에서는 네이티브 SDK 없이
`expo-auth-session` 브라우저 플로우로 받을 계획이라 Expo Go 에서 바로 테스트된다.

필요한 것: Kakao Developers 앱 등록 → REST API 키, 그리고 리다이렉트 URI 등록.

## 2.6 EAS 프로젝트 — 푸시가 이것 없이는 동작 안 함

`expo-notifications` 의 `getExpoPushTokenAsync()` 는 `app.json` 에 EAS `projectId`
가 있어야 토큰을 준다. 지금은 없어서 `registerPushToken()` 이 항상 `false` 를
돌려준다 (던지지는 않으므로 앱은 정상 동작).

즉 **알림 5종이 서버에는 다 구현돼 있는데 기기로 갈 수단이 없는 상태**다.
`eas init` 한 번이면 되고, Expo 계정만 있으면 무료다.

## 2.7 카카오 로그인 버튼 — **제출 차단 사유**

처음엔 "확인 필요"로 적었는데 최종 리뷰어가 분류를 올렸고 그게 맞다.
**카카오 로그인 가이드라인은 버튼의 색·로고·문구를 의무화한다.** 지금은 우리
디자인 토큰으로 그려놔서 (브랜드 색을 토큰 체계에 섞고 싶지 않았다) 위반 상태다.

제출 전에 반드시 카카오 공식 버튼 에셋으로 교체해야 한다. 디자인 시스템의
일관성보다 이쪽이 우선이다 — 우리 취향 문제가 아니라 남의 브랜드 규정이다.

## 3. 번들 ID와 앱 이름

- 현재 `com.studylog.app` 으로 잡아뒀다. 이미 쓰고 있거나 다른 걸 원하면 알려달라
- 앱 표시 이름은 `스터디로그` 로 뒀다 (셋로그 + 열품타에서 온 이름)

## 4. AWS 배포 — 같이 하기로 한 것

준비는 다 됐다. `server/deploy/runbook.md` 에 절차가 있고, 필요한 건:
- Lightsail t4g.small + RDS db.t4g.micro + S3 (월 $15~25 예상)
- **배포 전 `alembic check` 필수** — 마이그레이션이 손으로 작성됐고 진짜 Postgres에서
  한 번도 안 돌았다
- **FOR UPDATE 동시성이 실제 Postgres에서 미검증** — SQLite에선 락이 no-op이라
  테스트로 증명이 안 된다. 배포 후 확인해야 할 유일한 항목

## 5. 결정했지만 되돌릴 수 있는 것 (참고)

레저에 근거와 "틀렸을 때 비용"까지 적어뒀다. 특히 이 셋은 제품 방향이라 이견 있으면 말해달라.

- **홈의 가장 큰 숫자를 "돌려받은 돈"이 아니라 "아직 못 받은 돈"으로** — 손실회피 프레임.
  앱을 여는 것 자체가 부담스러워질 위험은 있다
- **되산 날(복구)은 완주로 안 쳐서 완주 보너스를 못 받는다** — 챌린지 막바지 복구 매출이 준다
- **전패해도 첫 챌린지 상한(3만원)이 풀린다** — 상한이 막으려던 건 실력이 아니라 경험 부족이라고 봤다

---

## 6. 판정 실측 결과 (2026-09-11, gpt-4o-mini)

선언 기반 판정으로 바꾼 뒤 실제 사진 10건을 돌렸다. **호출이 실제로 닿은 9건은 전부 정확**,
응답 1.5~3.5초. 실패한 1건은 판정 오류가 아니라 레이트 리밋이다(아래).

| 사진 | 선언 | 기대 | 결과 |
|---|---|---|---|
| 책상 위 책 | 공부 | 통과 | 통과 0.80 |
| 노트 필기 | 수학 문제집 풀기 | 통과 | 통과 0.70 |
| 노트북 코딩 | 코딩 | 통과 | 통과 0.90 |
| 도서관 | 책읽기 | 통과 | 통과 0.85 |
| 헬스장 | 운동 | 통과 | 통과 0.90 |
| 러닝 | 런닝머신 30분 | 통과 | 통과 0.70 |
| 헬스장 | **공부** | 거절 | 거절 0.90 |
| 책상 위 책 | **운동** | 거절 | 거절 0.90 |
| 음식 | 공부 | 거절 | 거절 0.95 |
| 풍경 | 책읽기 | 거절 | 거절 0.80 |

핵심은 아래 두 줄이다. **같은 사진이 선언에 따라 정반대로 판정된다** — 헬스장 사진은
"운동"이면 통과, "공부"면 거절. 선언 없이는 표현할 수 없던 판정이다.

**남은 위험 — 레이트 리밋이 곧 무조건 통과다.**
10건 중 1건이 429로 떨어졌고, 관대 폴백이 확신 0.00으로 통과시켰다. 설계대로지만
뒤집으면 **판정이 통째로 꺼지는 상황**이다. 유저가 몰리는 저녁 시간대에 이게 나면
그날 인증이 전부 공짜로 통과한다.

지금까지 한 것:
- 429·5xx 는 지수 백오프로 최대 3회 재시도한 뒤에만 폴백한다(10초 예산 안에서)
- SDK 자체 재시도(기본 2회)를 껐다 — 겹치면 한 번 판정에 최대 9번 호출이 나가고
  예산을 다 먹는다

**그래도 남는 것:** 이번 429는 조직 전체 TPM 한도(200k/분)가 이미 꽉 찬 상태라
재시도로 뚫리지 않았다. 테스트용 키를 다른 용도와 공유해서 생긴 일일 수도 있지만,
사진 판정은 이미지 토큰이 커서 실서비스에서도 TPM이 먼저 막힌다.

→ **네가 정해야 할 것:** 판정 모델을 gpt/claude/gemini 중 무엇으로 갈지, 그리고
   그 계정의 TPM 한도를 실제 동시 유저 수 기준으로 확인해야 한다. 저녁 피크에
   판정이 꺼지면 그날 매출이 전부 페이백으로 나간다.
